[project]
name = "ear_eeg_sound_lab"
version = "0.1.0"
description = "Sound-EEG closed-loop application"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.5",
    "scipy>=1.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
